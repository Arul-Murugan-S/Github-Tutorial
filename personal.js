console.log("Hello from Node!");

This personal.js is not added by .gitignore method in main branch but it has added by .gitignore from GitIgnore branch. So personal.js will appear in main branch but it will not appear in GitIgnore branch.